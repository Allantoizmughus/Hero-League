{{#partial "content"}}
    <div class="container">
        <h2><a CLASS="btn" href="/team-form">CREATE TEAM</a></h2>

        <div class="teams">
        {{#if teams}}
        <h2>HERE ARE THE TEAMS!</h2>
            {{#each teams}}
                <div>
                    <div class="card-body">
                    <h1>{{teamName}}</h1>
                    <p>Id: {{id}}</p>
                    <p>Purpose: {{cause}}</p>
                    <p>Max participants: {{ max_size }}</p>
                    </div>
                    <br><br>
                    <h2>MEMBERS</h2>
                    {{#each teamHeros}}
                            <div class="member-cont">
                                <h5>Name: {{this.name}}</h5>
                                <h5>Power: {{power}}</h5>
                            </div>
                        {{/each}}
                </div>
            {{/each}}
            {{else}}
            <p>Create a Team</p>
        {{/if}}
        </div>


    </div>
    {{/partial}}
{{>layout.hbs}}